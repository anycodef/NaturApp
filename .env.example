# URL base de la API del backend.
# Copia este archivo como .env y reemplaza el valor por la IP LAN de la
# máquina que ejecuta el backend (para dispositivos físicos), o usa
# localhost si trabajas con un emulador en el mismo host.
EXPO_PUBLIC_API_URL=http://YOUR_LAN_IP:9090/api
